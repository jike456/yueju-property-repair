<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.hello.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.example.hello.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="real_name" property="realName"/>
        <result column="phone" property="phone"/>
        <result column="id_card" property="idCard"/>
        <result column="avatar" property="avatar"/>
        <result column="role" property="role"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="last_login_time" property="lastLoginTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, username, password, real_name, phone, id_card, avatar,
        role, status, create_time, update_time, last_login_time
    </sql>

    <insert id="insert" parameterType="com.example.hello.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user
        (username, password, real_name, phone, id_card, avatar, role, status, create_time, update_time, last_login_time)
        VALUES
        (#{username}, #{password}, #{realName}, #{phone}, #{idCard}, #{avatar},
         #{role}, #{status}, #{createTime}, #{updateTime}, #{lastLoginTime})
    </insert>

    <update id="update" parameterType="com.example.hello.entity.User">
        UPDATE user
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="idCard != null">id_card = #{idCard},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="role != null">role = #{role},</if>
            <if test="status != null">status = #{status},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="lastLoginTime != null">last_login_time = #{lastLoginTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectById" parameterType="long" resultMap="UserResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        WHERE id = #{id}
    </select>

    <select id="selectByUsername" parameterType="string" resultMap="UserResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        WHERE username = #{username}
    </select>

    <select id="selectByPhone" parameterType="string" resultMap="UserResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        WHERE phone = #{phone}
    </select>

    <select id="countByUsername" parameterType="string" resultType="int">
        SELECT COUNT(1) FROM user WHERE username = #{username}
    </select>

    <select id="countByPhone" parameterType="string" resultType="int">
        SELECT COUNT(1) FROM user WHERE phone = #{phone}
    </select>

    <select id="selectByCondition" resultMap="UserResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        <where>
            <if test="role != null">
                AND role = #{role}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (
                    username LIKE CONCAT('%', #{keyword}, '%')
                    OR real_name LIKE CONCAT('%', #{keyword}, '%')
                    OR phone LIKE CONCAT('%', #{keyword}, '%')
                )
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="countByRoleExcludingId" resultType="int">
        SELECT COUNT(1) FROM user WHERE role = #{role} AND id != #{excludeId}
    </select>

    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>
</mapper>

